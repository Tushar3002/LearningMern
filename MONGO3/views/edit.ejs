<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Ur Chat</title>
</head>
<body>
    <p>Edit Chat : <%= chat._id %></p>
    <p>This Chatt was send from : <b><%= chat.from %></b> to: <b><%= chat.to%></b></p>

    <form action="/chats/<%= chat._id%>?_method=PUT" method="post">
        <textarea name="msg" rows="6" cols="30" id=""><%= chat.msg %></textarea><br>
        <button>Edit</button>
    </form>

</body>
</html>